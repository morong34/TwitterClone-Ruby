<%= form_with(model: user_name) do |form| %>
  <% if user_name.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_name.errors.count, "error") %> prohibited this user_name from being saved:</h2>

      <ul>
        <% user_name.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
